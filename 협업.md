# 협업 기록

## 개요

이 문서는 현재까지의 협업 내용과 합의된 기술 방향을 정리한 기록이다.

프로젝트는 Nexacro 17 Grid 메타데이터를 받아 서버에서 Excel 파일을 생성하는 Spring Boot 샘플이며, 협업 과정에서 런타임 기준, 날짜 파싱 정책, Excel 렌더링 확장 구조를 함께 정리했다.

## 주요 합의 사항

### 1. 응답 및 커뮤니케이션 방식

- 사용자 응답은 한글로 진행한다.
- 설명은 간결하게 하되, 구현 의도와 영향 범위는 분명히 정리한다.

### 2. 런타임 기준 변경

초기에는 프로젝트가 Java 17 / Spring Boot 3.3.x 기준으로 보였으나, 실제 운영 환경이 Java 8이라는 점을 반영해야 했다.

이에 따라 다음과 같이 기준을 변경했다.

- Spring Boot `3.3.5` → `2.7.18`
- Java `17` → `1.8`
- MyBatis Spring Boot Starter `3.0.3` → `2.3.2`
- `jakarta.servlet.*` → `javax.servlet.*`

추가로 Java 17 전용 문법도 제거했다.

- `record` 제거
- switch expression 제거
- pattern matching `instanceof` 제거

## DateParseUtil 협업 내용

### 1. 추가한 날짜/시간 패턴

다음 패턴을 추가 지원하기로 했다.

- `yyyy-MM-dd HH:mm`
- `HH:mm:ss`

또한 다음 정책을 유지하기로 했다.

- 4자리 숫자 입력은 기본적으로 `HHmm`으로 해석
- `yyMM`과 혼동 가능성은 있지만, 우선순위는 `HHmm`

### 2. 명시적 포맷 API 추가

다음 형태의 API를 추가했다.

- `format(input, inputPattern, outputPattern)`
- `format(input, DateTimeFormatter inputFormatter, String outputPattern)`
- `format(input, String inputPattern, DateTimeFormatter outputFormatter)`
- `format(input, DateTimeFormatter inputFormatter, DateTimeFormatter outputFormatter)`

### 3. 테스트 결과

다음 테스트를 통해 검증했다.

- `mvn -Dtest=DateParseUtilTest test` 성공
- 전체 `mvn test` 성공

## 문서 및 저장소 정리

### 1. AGENTS.md 초기화 및 운영 기준 반영

루트 `AGENTS.md`를 생성하고 프로젝트 작업 기준을 정리했다.

포함된 내용:

- 저장소 구조
- 변경 시 주의점
- Java 8 / Spring Boot 2.7 운영 기준
- `javax.servlet` 사용 원칙
- 테스트 실행 기본 명령

### 2. README 추가

`README.md`를 새로 작성했다.

포함된 내용:

- 프로젝트 개요
- Runtime Baseline
- 기능 설명
- 프로젝트 구조
- Build/Test 방법
- Run 방법
- Java 8 호환성 주의점
- Nexacro XAPI 관련 안내

### 3. 개발 환경 파일 제외

`.vscode/`는 저장소에 포함하지 않기로 했고, `.gitignore`에 추가했다.

## Excel Builder 확장 요구사항과 반영 내용

사용자가 추가로 요청한 요구사항은 아래 3가지였다.

1. Excel Builder가 `List` 기반 데이터뿐 아니라 `RowHandler`처럼 row 단위 처리도 할 수 있어야 한다.
2. Code 값을 Name으로 바꾸고, GridMeta의 날짜 포맷으로 문자열화하는 로직을 서비스별로 주입 가능해야 한다.
3. 데이터 값에 따라 셀 색상/정렬 등 스타일 로직도 서비스 단위로 주입 가능해야 한다.

이 요구사항을 반영하기 위해 구조를 확장했다.

### 1. 데이터 공급 방식 분리

추가된 구성 요소:

- `ExcelRowConsumer`
- `ExcelRowWriter`

적용 내용:

- 기존 `List<Map<String, Object>>` 방식 유지
- `ExcelRowWriter.fromList(...)` 제공
- MyBatis `ResultHandler`로 row 단위 처리 가능하도록 `SalesMapper.selectAllWithHandler(...)` 추가

### 2. 값 변환 정책 분리

추가된 구성 요소:

- `ExcelCellContext`
- `ExcelRenderPolicy`
- `DefaultExcelRenderPolicy`

정책 역할:

- `resolveCellValue(...)`에서 raw data를 실제 Excel 출력값으로 변환
- 기본 정책은 다음을 수행
  - `combo` 컬럼의 code → name 변환
  - `dateFormat`이 있으면 날짜 문자열 포맷 변환

즉, 서비스마다 다른 정책 객체를 주입하면 같은 builder를 쓰더라도 다른 출력 규칙을 적용할 수 있도록 했다.

### 3. 스타일 정책 분리

추가된 구성 요소:

- `ExcelCellStyleSpec`

정책 역할:

- 셀 정렬
- 배경색
- 숫자 포맷
- 테두리
- 폰트 bold
- 폰트 크기

등을 정책에서 override 가능하게 했다.

### 4. Builder 구현 변경

`NexacroGridExcelBuilder`는 다음을 지원하도록 확장했다.

- `List<Map<String, Object>>` 입력 기반 build
- `ExcelRowWriter` 기반 build
- `ExcelRenderPolicy` 주입 기반 build

또한 대량 데이터 처리를 고려해 `XSSFWorkbook`에서 `SXSSFWorkbook` 기반으로 변경했다.

### 5. Service 확장

`ExcelExportService`는 다음 형태를 지원하도록 확장했다.

- `exportExcel(columns, bands)`
- `exportExcel(columns, bands, ExcelRenderPolicy renderPolicy)`
- `exportExcel(columns, bands, List<Map<String, Object>> dataRows)`
- `exportExcel(columns, bands, List<Map<String, Object>> dataRows, ExcelRenderPolicy renderPolicy)`

즉,

- DB에서 row streaming으로 읽는 방식
- 이미 준비된 `List`를 직접 넘기는 방식
- 서비스별 정책을 주입하는 방식

을 모두 사용할 수 있게 했다.

## 테스트 결과

확장 작업 이후 다음 검증을 수행했다.

- `mvn test -Dtest=NexacroGridExcelBuilderTest,ExcelExportServiceTest,DateParseUtilTest` 성공
- `mvn test` 전체 성공

최종 결과:

- 총 테스트 `37`
- 실패 `0`
- 에러 `0`
- 스킵 `0`

## 현재 상태 요약

현재 프로젝트는 다음 상태다.

- 운영 기준: Java 8 / Spring Boot 2.7.18
- 날짜 파싱 유틸 보강 완료
- README 및 AGENTS 문서화 완료
- Excel Builder가 대량 데이터와 서비스별 출력 정책 주입을 지원하도록 확장 완료
- 전체 테스트 통과 완료

## 다음에 바로 이어서 할 수 있는 일

향후 이어서 할 수 있는 작업은 다음과 같다.

- 서비스별 `ExcelRenderPolicy` 구현체 예제 추가
- 상태값/금액값에 따른 셀 색상 정책 샘플 추가
- 날짜/숫자 포맷 전략을 업무 도메인별 정책 클래스로 분리
- 필요 시 `Cursor` 기반 스트리밍도 추가 검토
