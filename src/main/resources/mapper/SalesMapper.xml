<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.nexacro.mapper.SalesMapper">

    <!--
        List 기반 조회용 SQL.
        Map 결과에서는 map-underscore-to-camelCase가 자동 적용되지 않으므로
        ColumnMeta.colId와 맞추기 위해 alias를 camelCase로 명시한다.
        날짜는 문자열로 바로 내려 서비스/정책 없이도 기존 동작을 유지한다.
    -->
    <select id="selectAll" resultType="java.util.LinkedHashMap">
        SELECT
            ID              AS id,
            REGION_CD       AS regionCd,
            CATEGORY_CD     AS categoryCd,
            Q1_SALES        AS q1Sales,
            Q1_QTY          AS q1Qty,
            Q2_SALES        AS q2Sales,
            Q2_QTY          AS q2Qty,
            TOTAL_SALES     AS totalSales,
            TO_CHAR(REG_DATE, 'YYYY-MM-DD') AS regDate
        FROM SALES_DATA
        ORDER BY ID
    </select>

    <!--
        RowHandler 기반 streaming 조회용 SQL.
        날짜 원본값을 그대로 내려 정책 계층에서 업무별 포맷을 적용할 수 있게 한다.
    -->
    <select id="selectAllWithHandler" resultType="java.util.LinkedHashMap">
        SELECT
            ID              AS id,
            REGION_CD       AS regionCd,
            CATEGORY_CD     AS categoryCd,
            Q1_SALES        AS q1Sales,
            Q1_QTY          AS q1Qty,
            Q2_SALES        AS q2Sales,
            Q2_QTY          AS q2Qty,
            TOTAL_SALES     AS totalSales,
            REG_DATE        AS regDate
        FROM SALES_DATA
        ORDER BY ID
    </select>

</mapper>
